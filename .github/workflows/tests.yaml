name: Run tests gmp examples

on:
  push:
    branches:    
      - feat/run-tests 

jobs:
  release-binaries:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-22.04]
        arch: [amd64]

    steps:

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Prerequisites
        run: |
          wget --no-check-certificate https://github.com/aptos-labs/aptos-core/blob/main/scripts/dev_setup.sh
          chmod +x dev_setup.sh
          ./dev_setup.sh

      - name: Build
        run: |
          npm install
          npm run build

      - name: Test
        run: |
          npm run test

